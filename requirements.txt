# Factor-Alpha Screener — Python dependencies

# --- core analytics ---
pandas
numpy
statsmodels        # OLS regression (alpha, betas, Newey-West / HAC standard errors)
yfinance           # prices, returns, market cap, factor / driver series
pyyaml             # config.yaml

# --- charts (server-side exports saved with a run) ---
plotly

# --- API server (serves the built Next.js front-end + /api at one origin) ---
fastapi
uvicorn[standard]  # ASGI server (production start command)
pydantic           # request / response models

# --- tests ---
pytest

# Notes:
# - The AI-exposure agent + the "how to read" explainer use the `gemini` CLI as a $0 subprocess
#   (no pip dependency). For a headless / cloud path, set GEMINI_API_KEY to use the REST API instead.
# - The front-end (web/) is a Next.js app built with Node; see README.md. The built static export
#   (web/out) is committed, so the app also runs with Python alone.
