# Local dev only: the Next dev server (:3000) calls the FastAPI backend (:8000) directly.
# Production builds do NOT use this file — there BASE='' (same origin, FastAPI serves the app).
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
